#include <stdio.h>

int main(void) {

	printf("I'm running in a container in an app built with C and I do not even need a distribution!\n");
	printf("Now With New Features MULTILINE (note this is paid feature!)\n");
	return (0);
}
